<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Strict//EN"
    "http://www.w3.org/TR/xhtml1/DTD/xhtml1-strict.dtd">
<html xmlns="http://www.w3.org/1999/xhtml" xml:lang="en" lang="en">
 <% if @stats.blank? %>
<p>There are not any driving stats currently in the system.</p>
<% else %>
<p>These are the current driving stats in our system</p>
<ul id="driving_stats">
<% @stats.each do |c| %>
<li>
<%= link_to c.iphone_timestamp, {:action => 'show', :id => c.id} -%>
<b> <%= link_to "Delete", {:action => 'delete', :id => c.id},
:confirm => "Are you sure you want to delete this item?" %></b>
</li>
<% end %>
</ul>
<% end %>
<p><%= link_to "Add new driving stat", {:action => 'new' }%></p>
</html>
